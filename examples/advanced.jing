// Advanced Jing Example: Simple Calculator with Functions

fn add(a, b) {
    return a + b;
}

// Main program
print("=== Jing Calculator ===");

let x = 10;
let y = 5;

print("Addition:");
let result = add(x, y);
print(result);
